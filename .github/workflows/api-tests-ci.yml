name: API Tests CI

permissions:
  contents: read
  packages: write

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  api-tests-quality:
    name: API Tests Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required files
        run: |
          test -d api-tests
          test -f api-tests/Dockerfile
          test -f docker-compose.yml

      - name: Run dockerized API acceptance tests (source)
        run: |
          docker compose --profile tests up --build --abort-on-container-exit --exit-code-from api-tests api-tests

      - name: Tear down source stack
        if: always()
        run: docker compose --profile tests down -v

  api-tests-image:
    name: API Tests Image
    runs-on: ubuntu-latest
    needs: api-tests-quality
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and optionally publish api-tests image
        uses: docker/build-push-action@v6
        with:
          context: ./api-tests
          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/triangleoflove-api-tests:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/triangleoflove-api-tests:latest

  ghcr-integration:
    name: API Tests Against GHCR Images
    runs-on: ubuntu-latest
    needs: api-tests-image
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      REPOSITORY_OWNER: ${{ github.repository_owner }}
      IMAGE_TAG: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate GHCR compose file
        run: test -f .github/compose/ghcr-integration.compose.yml

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull GHCR images
        run: |
          docker pull ghcr.io/${REPOSITORY_OWNER}/triangleoflove-db:${IMAGE_TAG}
          docker pull ghcr.io/${REPOSITORY_OWNER}/triangleoflove-backend:${IMAGE_TAG}
          docker pull ghcr.io/${REPOSITORY_OWNER}/triangleoflove-frontend:${IMAGE_TAG}
          docker pull ghcr.io/${REPOSITORY_OWNER}/triangleoflove-api-tests:${IMAGE_TAG}

      - name: Start services from GHCR images
        run: |
          docker compose -f .github/compose/ghcr-integration.compose.yml up --abort-on-container-exit --exit-code-from api-tests api-tests

      - name: Tear down GHCR test stack
        if: always()
        run: docker compose -f .github/compose/ghcr-integration.compose.yml down -v
